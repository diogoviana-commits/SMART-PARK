package com.smartparkuscs.mapbackend.model;

/** Como o progresso de uma missao e medido (RF10). */
public enum TipoMissao {

    /** Distancia percorrida, em quilometros. */
    DISTANCIA,

    /** Visitas registradas em pontos de interesse. */
    CHECKIN,

    /** Participacao em eventos do parque. */
    EVENTO
}
